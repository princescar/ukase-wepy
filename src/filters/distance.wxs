module.exports = {
  format: function(distance) {
    if (distance > 1000) {
      return parseInt(distance / 1000 * 100) / 100 + ' km'
    } else {
      return parseInt(distance) + ' m'
    }
  }
}
