let systemInfo
let menuButtonRect

export default {
  set systemInfo(value) {
    systemInfo = value
  },
  get systemInfo() {
    return systemInfo
  },
  set menuButtonRect(value) {
    menuButtonRect = value
  },
  get menuButtonRect() {
    return menuButtonRect
  }
}
